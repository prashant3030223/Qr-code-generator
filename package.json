{
  "name": "qrpulse-studio",
  "version": "1.0.0",
  "description": "Premium Mobile-First Static QR Code Generator",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run preview --prefix frontend"
  }
}
